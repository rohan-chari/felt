# Copy to .env (gitignored) for local dev.

# Server
PORT=8080
CORS_ORIGIN=http://localhost:5173

# Postgres — must match docker-compose.yml
POSTGRES_USER=felt
POSTGRES_PASSWORD=felt
POSTGRES_DB=felt
DATABASE_URL=postgres://felt:felt@localhost:5432/felt
